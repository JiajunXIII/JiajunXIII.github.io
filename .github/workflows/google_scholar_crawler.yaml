name: Google Scholar statistics (disabled in V1)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled workflow
        run: echo "Google Scholar statistics are disabled until a verified Scholar profile is configured."
